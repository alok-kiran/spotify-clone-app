/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['images.unsplash.com', 'dmgljstlyggbtexgnzkp.supabase.co'],
    },
}

module.exports = nextConfig
